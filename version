local Players = game:GetService("Players")
 
 
if _G.SystemFound ~= true then
    for i,v in pairs(Players:GetPlayers()) do
        if v.Name == "Oderi" then
            game:GetService("StarterGui"):SetCore("SendNotification", {Title = "System note",Text = "Remastere by W1r.",Duration = 7;})
            _G.SystemFound = true
        end
    end
end
 
if Players.LocalPlayer.Name == "DimaRBX97" then
    for i = 1,6 do
        local args = {[1] = "Bye bye <3",[2] = "All"}
        game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(unpack(args))
    end
    task.wait(1)
    Players.LocalPlayer:Kick("Напиши в тг W1ryyx")
end
 
return 2
